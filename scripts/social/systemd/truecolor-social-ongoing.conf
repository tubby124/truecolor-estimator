# Install as truecolor-social.service.d/ongoing.conf only after final exact approval.
# Keeps ONE posting timer and the existing credential/runner-lock boundary.
[Service]
ExecStart=
ExecStart=/usr/bin/python3 /opt/truecolor-social/vps-scheduler.py --config /etc/truecolor-social/ongoing.json --run

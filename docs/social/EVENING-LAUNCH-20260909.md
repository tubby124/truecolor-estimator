# Daily evening creative — execution handoff

Owner clarification, September 9, 2026: the existing September morning creative campaign predates this harness and must remain exactly as approved. Add one creative daily at **19:00 America/Regina**, creating two creatives per day in total. Facebook and Instagram are two destinations for each creative, not two different daily creatives. This direction supersedes the older three-extra-per-week plan and any assumption that the morning campaign is exclusively customer work.

## Prepared milestone and next task

A Terra High worker reused the existing connected preparation workflow and three retained raw sources. The private consecutive-date canary is September 10, 11 and 12 at 19:00 Regina, corresponding to September 11, 12 and 13 at 01:00 UTC. It contains a window-decals showcase, business-card artwork tip and candle-label use case: three creatives, six Meta destinations. Exact media/captions/accounts/times require normal review and approval. Rebase any elapsed date before approval; never silently roll an already approved schedule forward.

The package is **prepared, not uploaded, approved, enrolled or published**. The private operational handoff owns review media, input hashes, connected receipt and launch prompt. Never put private captions/metrics, access-bearing links, account exports, IDs or secrets in this public repository.

## Existing system verified September 10, 01:27 UTC

Read-only VPS configuration plus authenticated scheduler check/receipt calls verified 48 existing destination IDs, the same scope, active sole runner and monitor timers, publishing enabled, no hold, zero due work and no backlog/stale/pending flag. Optional intake enrollment is absent/default false. Six saved posted receipts since September 7 have morning local times of 09:00, 10:00 and 10:30. This does not independently verify all six provider objects or future September delivery.

Monitor configuration remains `posted_only`. At 01:28 UTC its scope-bound notification journal had twelve events, all acknowledged, no pending/in-flight/uncertain entries. These are twelve journal events, not a claim of twelve posts or that the owner read the messages. No dispatch, Telegram test, queue/config change or manual retry was performed during this audit.

## Execution through the existing harness

1. Read current source, [connected preparation](CONNECTED-PREPARATION.md), [batch bridge](HARNESS-BATCH-BRIDGE.md), [launch operations](LAUNCH-OPERATIONS.md), [monitor runbook](ONGOING-MONITOR-PREPARATION-20260906.md) and the private package. Refresh the full AM calendar and exact IDs/hashes immediately before writing. Preserve its media, captions, accounts, approvals and staggered times.
2. Reuse the prepared consecutive-date package. Import into the existing monthly review; verify exact final bytes, copy, channel, timezone and no same-channel/time collision. Read back the six returned destination IDs and get normal exact owner approval through authenticated staff workflow. Do not bypass approval through SQL or service credentials.
3. Prepare one reconciled enrollment adding those six IDs to the unchanged existing 48. App `SOCIAL_ONGOING_POST_IDS` and VPS `ongoing.json` must agree; 54 remains under the current 100-ID base cap. Approved rows are not automatically enrolled. Generic monthly creatives must not be forced through photo intake.
4. Perform the scoped cutover under the existing runbook: preserve and reconcile runner/monitor scope-bound journals, including notification history. Never delete/reset them to clear a mismatch. Keep one posting timer and the existing independent monitor. Verify no AM destination changed and the read-only check is healthy before allowing its next normal tick. Do not manually dispatch as a configuration test.
5. After the first PM pair, reconcile app receipts to exact provider objects and public links, duplicate status and saved Telegram acknowledgment. A successful dispatch HTTP response is not publication. An uncertain result requires reconciliation before retry. Once this pair is proven, expand live enrollment in bounded reviewed batches. Later content preparation and review can proceed while waiting; there is no artificial three-day production freeze.
6. Target daily PM through September 30 with distinct reviewed content. Check remaining capacity before enrollment: September 10–30 adds 42 destinations; together with existing 48 this is 90 before any other additions. This is planning arithmetic, not a live reservation or approval. Refresh counts and do not enable intake or widen capacity as a shortcut.

## What the harness learns

Use [the existing learning loop](LEARNING-LOOP.md), not a second data store or scheduler. Delivery timing, provider links, failures and duplicate reconciliation describe delivery. Owner feedback records crop/logo/caption decisions against recipe/version. The existing bounded `meta_engagement.py` GET collector can create private snapshots/events; its dated proof returned Instagram likes/comments, while several insights lacked permission. Missing metrics remain null. Collection and ingestion are explicit operator steps, not a hosted recurring observer.

The connected preparation path applies accepted scoped learning to deterministic image branding/rendering and carries lineage to review. Current draft captions are supplied inputs; automated learned caption generation is still a gap. New audience observations may propose a lesson; they do not prove sales or authorize automatic recipe/approval changes. Demonstrate one bounded delivery → available observation → owner-reviewed lesson → next prepared draft cycle before claiming the full loop automatic.

## Faster working model

Default the next bounded implementation task to **Terra High** with this handoff and exact private package. This task already used Terra High successfully for preparation; it is not a broad model-quality benchmark. Keep checks deterministic and have the owner review the final creative. Reserve Astra for a concrete unresolved design/security/factual issue, not routine orchestration. Luna can be evaluated later on one reversible, reviewable drafting/documentation task; no evidence here establishes its release reliability.

Do not restart discovery, regenerate the approved sources, replace the September AM plan, recreate working auto-posting infrastructure, or wait for an expensive model to repeat a completed audit. Refresh only facts needed for the next mutation. Save sanitized completion evidence here and private exact IDs/metrics in the existing operational package; the Vault is a pointer/brief, never a duplicate queue.

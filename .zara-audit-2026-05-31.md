# True Color System Audit — Restart Point
# 2026-05-31 ~19:15 MT

## Architecture (confirmed with Hasan)
- **Clover** = payment processor (card capture)
- **Wave** = accounting engine (invoices, tax receipts, bookkeeping)
- Pipeline: Customer pays Clover → webhook fires → marks order paid → approves Wave invoice → records Wave payment → sends receipt with paid Wave PDF link
- Wave is NOT abandoned. It's still the bookkeeping backbone. Only the payment rail changed.

## What we checked
- Repo: tubby124/truecolor-estimator (main, commit 8611045)
- Deploy: Railway (project 98b1de15, service e909053b)
- Live site: up, all pages load, health endpoint clean (1 low-severity warn)
- Build: passes with NODE_OPTIONS=--max-old-space-size=4096
- Tests: 170/170 passing (fixed Telegram test env leak)

## Reconciler findings (from GH Actions run 26719166582, 2026-05-31 17:15 UTC)
```json
{"issues_total":33,"auto_recovered":0,"by_kind":{"wave_invoice_missing":1,"wave_orphan":0,"clover_stuck":6,"wave_recovery_failed":0,"wave_payment_zombie":26,"wave_recovery_unverified":0,"wave_approve_failed":0}}
```

### 26 wave_payment_zombie
DB says wave_payment_recorded_at is set, but Wave API shows invoice underpaid.
These are real — customers were charged but tax invoice shows unpaid.
Manual fix: open each Wave invoice, record payment manually.
Likely from pre-May-22nd bug where approve failure blocked recordPayment.

### 6 clover_stuck
Abandoned Clover checkouts >24h old (pending_payment status).
Not bookkeeping problems.

### 1 wave_invoice_missing
One Wave-method order without a linked invoice ID.

## What's working
- Clover webhook → order status: ✓
- Clover webhook → Wave approve: ✓ (0 failures)
- Clover webhook → Wave payment recording: ✓ for new orders
- Reconciler auto-healing: ✓ (just nothing needed it)
- Health endpoint: clean
- Build/tests: all green

## Fixed in this session
- Stale repo checkout: reset to origin/main
- Test isolation: Telegram tests now blank Supabase env vars (commit 8611045)
- CI: passed

## Known non-urgent
- GA4 Daily Sync cron fails (missing GA4_SERVICE_ACCOUNT_JSON)
- PAYMENT_TOKEN_SECRET is low entropy (don't rotate during active pay links)

## Next steps for next session
1. Hasan: decide if we want to triage the 26 zombies (Telegram alert already fires daily with order numbers)
2. GA4 cron: either add env var or disable workflow
3. The system is stable — nothing needs breaking

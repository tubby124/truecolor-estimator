import { MobileUploadClient } from "./MobileUploadClient";

export const metadata = {
  title: "Upload | Social Studio",
};

export default function MobileUploadPage() {
  return <MobileUploadClient />;
}

// Phase 1 — Staff Estimator lives here (migrating from / when public landing is ready)
// TODO Phase 2: Move estimator from page.tsx to this route and add auth gate
export default function StaffPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-sm text-gray-400">Staff estimator — coming to /staff in Phase 2</p>
    </div>
  );
}

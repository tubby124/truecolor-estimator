// Phase 2 — Save/retrieve quotes (Supabase)
// POST /api/quote → save EstimateResponse → return { id }
export async function POST() {
  return Response.json({ error: "Not yet implemented" }, { status: 501 });
}
